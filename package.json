{
  "name": "bookshop",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^8.8.3",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "mocha": "^10"
  },
  "scripts": {
    "test": "mocha test/*.test.js"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "sqlite:sqlite.db"
        }
      }
    }
  }
}
